__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
*.log

# Node / Next.js frontend
node_modules/
.next/
*.tsbuildinfo
next-env.d.ts

# git worktrees for isolated feature work
.worktrees/
.superpowers/
.claude/worktrees/

# Playwright MCP session snapshots (local debugging artefacts)
.playwright-mcp/

# Telethon login session (contains an authenticated Telegram session)
*.session
*.session-journal

# Provisoire : ce script porte un TELEGRAM_API_HASH en dur comme valeur par
# defaut, donc un `git add -A` le publierait. A retirer d'ici quand il aura ete
# soit supprime, soit aligne sur scripts/telegram_session.py, qui refuse de
# demarrer sans ses variables d'environnement.
scripts/telegram_channel_id.py
