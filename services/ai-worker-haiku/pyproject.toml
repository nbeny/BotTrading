[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ai-worker-haiku"
version = "0.1.0"
description = "Claude Haiku triage worker — publishes market.analysis.events"
requires-python = ">=3.12"
dependencies = ["cmi-common", "anthropic>=0.34"]

[tool.hatch.build.targets.wheel]
packages = ["app"]
