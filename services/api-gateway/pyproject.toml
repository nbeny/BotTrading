[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "api-gateway"
version = "0.1.0"
description = "REST API gateway + event persister"
requires-python = ">=3.12"
dependencies = ["cmi-common"]

[tool.hatch.build.targets.wheel]
packages = ["app"]
