[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "collector-twitter"
version = "0.1.0"
description = "Twitter/X social collector — publishes market.social.events"
requires-python = ">=3.12"
dependencies = ["cmi-common", "httpx>=0.27"]

[tool.hatch.build.targets.wheel]
packages = ["app"]
