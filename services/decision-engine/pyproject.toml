[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "decision-engine"
version = "0.1.0"
description = "Deterministic scoring engine — publishes decision.events"
requires-python = ">=3.12"
dependencies = ["cmi-common"]

[tool.hatch.build.targets.wheel]
packages = ["app"]
